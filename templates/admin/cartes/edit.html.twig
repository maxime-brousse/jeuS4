{% extends 'admin.html.twig' %}

{% block title %}Carte | éditer{% endblock %}

{% block body %}
    <div class="content">
        <h1 class="contenth1">Modifier une Cartes</h1>

        {{ include('admin/cartes/_form.html.twig', {'button_label': 'enregistrer'}) }}

        <a href="{{ path('cartes_index') }}"><button class="btn btn-secondary btn-lg">retour aux cartes</button></a>

        {{ include('admin/cartes/_delete_form.html.twig') }}
    </div>
{% endblock %}
